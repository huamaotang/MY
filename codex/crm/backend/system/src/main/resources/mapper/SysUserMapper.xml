<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.crm.mapper.SysUserMapper">
    <resultMap id="BaseResultMap" type="com.example.crm.entity.SysUser">
        <id column="id" property="id"/>
        <result column="dept_id" property="deptId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="mobile" property="mobile"/>
        <result column="email" property="email"/>
        <result column="status" property="status"/>
        <result column="last_login_at" property="lastLoginAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="BaseColumnList">
        id, dept_id, username, password, real_name, mobile, email, status, last_login_at, created_at, updated_at
    </sql>

    <select id="findByUsername" resultMap="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from sys_user
        where username = #{username}
        limit 1
    </select>

    <select id="selectRoleCodes" resultType="java.lang.String">
        select r.role_code
        from sys_role r
        inner join sys_user_role ur on ur.role_id = r.id
        where ur.user_id = #{userId}
          and r.status = 1
    </select>

    <select id="selectPermissionCodes" resultType="java.lang.String">
        select distinct m.permission_code
        from sys_menu m
        inner join sys_role_menu rm on rm.menu_id = m.id
        inner join sys_user_role ur on ur.role_id = rm.role_id
        where ur.user_id = #{userId}
          and m.visible = 1
          and m.permission_code is not null
    </select>
</mapper>
