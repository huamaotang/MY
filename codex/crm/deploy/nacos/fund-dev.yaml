server:
  port: 8784
  shutdown: graceful

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "${FUND_MYSQL_URL:jdbc:mysql://localhost:3306/fund?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai}"
    username: "${FUND_MYSQL_USER:${MYSQL_USER:root}}"
    password: "${FUND_MYSQL_PASSWORD:${MYSQL_PASSWORD:qwer8989}}"

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

crm:
  jwt:
    secret: "${CRM_JWT_SECRET:change-this-nacos-development-secret}"
    expire-seconds: "${CRM_JWT_EXPIRE_SECONDS:86400}"
  access-log:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,serviceregistry
